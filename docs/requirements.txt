# Documentation build requirements (Read the Docs + local builds).
sphinx>=7
myst-parser>=2
sphinx-rtd-theme>=2
# marimo: exports the interactive model explorers to WebAssembly at build time
# (docs/export_interactive.py). uv is needed on PATH for the export.
marimo>=0.24
uv>=0.7
